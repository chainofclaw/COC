{
  "name": "coc-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "coverage:check": "hardhat coverage && node scripts/check-coverage.js",
    "deploy:local": "hardhat run scripts/deploy-posemanager.js --network hardhat",
    "deploy:governance": "hardhat run scripts/deploy-governance.js --network prowl",
    "deploy:governance:local": "hardhat run scripts/deploy-governance.js --network hardhat",
    "verify:pose": "hardhat run scripts/verify-pose.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.8",
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "chai": "^4.5.0",
    "ethers": "^6.13.5",
    "hardhat": "^2.22.18",
    "hardhat-gas-reporter": "^2.2.1",
    "solidity-coverage": "^0.8.13"
  }
}
